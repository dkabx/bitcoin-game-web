const urlConstants = {
  getBitcoinPrice: 'api/v1/assets/bitcoin/metrics',
  getScore: 'dev/get-score',
  updateScore: 'dev/update-score',
};

//font family
const fontFamily = 'mulish';

const GAME_TIMEOUT_VALUE = 60;

export { fontFamily, urlConstants, GAME_TIMEOUT_VALUE };
